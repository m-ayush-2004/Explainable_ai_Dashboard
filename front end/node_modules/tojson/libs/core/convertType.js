// Generated by CoffeeScript 1.8.0
(function() {
  var getItem, getType;

  exports.getType = getType = function(str) {
    if (str.indexOf('#Number') !== -1) {
      return 'Number';
    } else if (str.indexOf('#Boolean') !== -1) {
      return 'Boolean';
    }
    return null;
  };

  exports.getItem = getItem = function(type, item) {
    if (type === 'Number') {
      item = parseFloat(item);
    }
    if (type === 'Boolean') {
      if (item === 'True' || item === 'TRUE' || item === 'true') {
        item = true;
      } else if (item === 'False' || item === 'FALSE' || item === 'false') {
        item = false;
      }
    }
    return item;
  };

}).call(this);
